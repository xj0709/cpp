#binaryTreeRecursive.cpp
* use c style code struct to create a binary tree, insert node, and traversal tree recursively
* use class to create a binary tree, insert node, and traversal tree recursively
